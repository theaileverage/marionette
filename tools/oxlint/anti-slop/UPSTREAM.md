# Vendored anti-slop

Source: https://github.com/dmmulroy/anti-slop

Revision: `e8c4880471b23ab7f216fba7b27d173a6ef07d4c`

Copied with upstream's `skills/install-anti-slop/scripts/install.mjs` on 2026-09-08.
The generic and Effect plugins are enabled in the repository's `.oxlintrc.json`.
The original MIT license is included in `LICENSE`.

These files are maintained as vendored source. Review upstream changes and run
`npm run lint` and `npm run tooling:check` when updating them. Keep `oxlint` and
`@oxlint/plugins` pinned to the same version, compatible with `@effect/tsgo`.
